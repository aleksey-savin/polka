-- Повторный проход воркера: ранний фильтр по языку оригинала произведения
-- выкидывал всю библиографию переводных авторов (Несбё и т.п.), а био
-- сохранялось с BB-тегами. Возвращаем выполненные задачи в очередь.
UPDATE `crawl_task`
SET `status` = 'pending', `attempts` = 0, `scheduled_at` = unixepoch(), `error` = NULL
WHERE `status` IN ('done', 'failed');
